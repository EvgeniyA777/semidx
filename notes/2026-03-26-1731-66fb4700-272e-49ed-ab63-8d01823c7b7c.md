---
file_type: working-note
topic: elixir-tree-sitter-baseline
created_at: 2026-03-26T17:31:31-0700
author: codex
language: en
status: active
---

# Working Note: Elixir Tree-Sitter Baseline Harness

This note captures the pre-refactor Elixir benchmark baseline before the tree-sitter extractor work begins.

## Commands

Regex-only Elixir benchmark subset:

```bash
./scripts/run-benchmarks.sh --fixture-prefix retrieval_elixir_ --elixir-engine regex
```

Regression suite:

```bash
clojure -M:test
```

## Observed Baseline

Date:

`2026-03-26T17:31:31-0700`

Benchmark subset result:

- `retrieval_elixir_aliased_local_ambiguity_001` failed: missing warning code `target_ambiguous`
- `retrieval_elixir_mixed_def_forms_001` failed: missing top authority unit `lib/my_app/order.ex::MyApp.Order/to_status`
- `retrieval_elixir_exunit_module_scenario_001` failed: rank band expectation mismatch

Summary:

- benchmark subset: `total=3 passed=0 failed=3`
- `clojure -M:test`: passed (`158 tests`, `984 assertions`, `0 failures`, `0 errors`)

## Why This Exists

Stage 0 adds a reproducible Elixir-only benchmark gate so later tree-sitter refactors can be compared against an explicit starting point instead of against memory.

This note is the comparison baseline for the later Elixir tree-sitter readiness stages.
