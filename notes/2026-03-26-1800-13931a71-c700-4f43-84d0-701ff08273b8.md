---
title: "Elixir Tree-Sitter Readiness Status"
doc_type: "status_note"
lifecycle: "completed"
agent_action: "historical_reference_only"
updated: "2026-08-02"
file_type: working-note
topic: elixir-tree-sitter-readiness
created_at: 2026-03-26T18:00:16-0700
author: codex
language: en
status: completed
---

# Working Note: Elixir Tree-Sitter Readiness Status

This artifact refreshes the current Elixir tree-sitter tranche with an explicit readiness status based on rerun verification rather than on the earlier blocked state.

## Delivered in This Tranche

- Stage 0 baseline harness landed
- Stage 1 Elixir boundary extraction landed
- Stage 2 Elixir tree-sitter structural extraction landed

## Current Readiness

Status:

`not-ready-for-default-switch`

Current decision:

- keep Elixir tree-sitter as an optional path
- do not flip default parser yet

## Green Evidence

- env-enabled parser-path verification is now green
- env-enabled Elixir tree-sitter parity verification is now green
- plain `clojure -M:test` remains green after the tree-sitter invocation fixes
- Elixir now has a dedicated tree-sitter provider instead of a validation-only wrapper over the full regex provider result

Observed commands:

```bash
SCI_TREE_SITTER_CLOJURE_GRAMMAR_PATH=$PWD/.tree-sitter-grammars/tree-sitter-clojure \
SCI_TREE_SITTER_ELIXIR_GRAMMAR_PATH=$PWD/.tree-sitter-grammars/tree-sitter-elixir \
SCI_TREE_SITTER_JAVA_GRAMMAR_PATH=$PWD/.tree-sitter-grammars/tree-sitter-java \
SCI_TREE_SITTER_TYPESCRIPT_GRAMMAR_PATH=$PWD/.tree-sitter-grammars/tree-sitter-typescript/typescript \
clojure -Sdeps '{:paths ["src" "test"]}' -e '...tree-sitter-parser-path-test + tree-sitter-elixir-parity-test...'

clojure -M:test
```

Observed results:

- env-enabled targeted tree-sitter checks: `{:test 2, :pass 12, :fail 0, :error 0}`
- full regression suite: `158 tests`, `984 assertions`, `0 failures`, `0 errors`

## Resolved Since Previous Revision

1. Shared tree-sitter parser-path verification is no longer red.

Resolved shape:

- `tree-sitter-parser-path-test` now passes under env-enabled execution
- the earlier CLI/config invocation problem in the shared tree-sitter path is no longer an active blocker

2. Elixir-specific parity verification is no longer red.

Resolved shape:

- `tree-sitter-elixir-parity-test` now passes under env-enabled execution
- the current Elixir tree-sitter path clears the targeted parity gate required for readiness tracking

## Remaining Readiness Gaps

1. Elixir benchmark subset is still red in both parser modes.

Current baseline remains:

- `retrieval_elixir_aliased_local_ambiguity_001`
- `retrieval_elixir_mixed_def_forms_001`
- `retrieval_elixir_exunit_module_scenario_001`

Current evidence:

- regex mode still fails all three fixtures
- tree-sitter mode fails the same three fixtures with the same failure modes
- this is baseline debt, not a tree-sitter-specific regression

Why it still matters:

- the benchmark subset does not yet provide positive evidence for a default switch
- identical debt across both modes is enough to avoid opening a remediation tranche right now, but not enough to approve tree-sitter-by-default

## Next Required Work Before Any Default Flip

- keep Elixir tree-sitter optional
- treat the shared invocation blocker as closed
- use the benchmark delta note as the evidence source for whether a remediation tranche is needed
- require a later positive retrieval-evidence tranche before any default-switch decision

## Decision Record

The current rollout does not reopen a parser-infrastructure tranche.

The next Elixir tree-sitter work should be either:

- a focused Elixir benchmark/remediation tranche only if a future benchmark delta shows tree-sitter-specific regression, or
- a later default-readiness artifact once retrieval evidence is stronger than the current shared baseline debt
