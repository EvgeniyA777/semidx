name: Contracts Validation

on:
  pull_request:
    paths:
      - "contracts/**"
      - "fixtures/**"
      - "src/semidx/contracts/**"
      - "deps.edn"
      - "scripts/validate-contracts.sh"
      - ".github/workflows/contracts-validation.yml"
  push:
    # `dev` is the integration branch; see the note in mvp-runtime.yml.
    branches: ["main", "master", "dev"]
    paths:
      - "contracts/**"
      - "fixtures/**"
      - "src/semidx/contracts/**"
      - "deps.edn"
      - "scripts/validate-contracts.sh"
      - ".github/workflows/contracts-validation.yml"

# Collapses the duplicate push/pull_request pair on the same content; see the
# note on the same block in mvp-runtime.yml for why the key is `head_ref` rather
# than `ref`.
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  validate-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Clojure CLI
        uses: DeLaGuardo/setup-clojure@13.2
        with:
          cli: "1.12.0.1530"

      - name: Validate Contracts
        run: ./scripts/validate-contracts.sh
