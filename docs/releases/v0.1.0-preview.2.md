---
title: "v0.1.0-preview.2 release notes"
doc_type: "reference"
lifecycle: "active"
status: "active"
agent_action: "reference_for_context"
updated: "2026-09-17"
---

# v0.1.0-preview.2 Release Notes

`v0.1.0-preview.2` is the second local MCP preview release candidate for
semidx. It is the [v0.1.0-preview.1](v0.1.0-preview.1.md) surface plus the Zig
coverage of
[Plan 006](../plans/006_zig_dogfood_semantic_coverage.md). The product version
was raised because that coverage changes what the same tools answer.

It is intended for the maintainer and local coding agents. It is not a stable
semantic contract, not a packaged multi-platform binary release, and not a
promise of broad language coverage.

## What Changed Since preview.1

Zig coverage, under
[ADR 006](../adr/006_allow_narrow_zig_member_definitions_and_local_import_calls.md):

- A named `fn` declared directly inside a top-level Zig container is a
  `function` definition with a `container_path`, introduced by its container.
  `semidx_repo_map` counts it as nested, and `semidx_find_definitions` and
  `semidx_context` return it. Its body is analyzed for calls.
- `alias.foo(...)` is a cross-unit `calls` fact when `alias` is a top-level
  `const alias = @import("relative/path.zig")` naming one indexed Zig unit that
  exports exactly one top-level `pub fn foo`. The importer declares a dependency
  on that unit, so provider edits, renames, visibility changes, broken analysis,
  moves, and removals update the importer without editing it.
- Every other qualified call stays an unresolved designator with a reason, and
  a `.zig` import that establishes no alias is reported as an unsupported
  construct with its reason.

Graph maintenance:

- A scan registers every added unit before analyzing any, and reanalyzes a unit
  that read a provider analyzed later in the same scan, so the graph does not
  depend on file order. A moved unit reanalyzes the units that found it by path.
- Renaming a container reports its members' identity loss with their
  replacements, for every language, instead of a removal and a creation.

MCP output shape and tool names are unchanged; `semantic_contract_version` is
still `null`. Answers differ where the coverage above applies: more Zig
definitions, cross-unit callers in `semidx_references`, and larger
`semidx_context` responses for call-heavy Zig functions.

## What This Preview Provides

- A source-built `semidx-mcp` binary.
- One binary usable against many local repository roots with `--root`.
- Local stdio MCP tools for graph-backed health, repository maps, definition
  lookup, references/calls, focused graph context, and refresh.
- Product version reporting as `0.1.0-preview.2`.
- `semantic_contract_version: null` in MCP structured results.
- Java, Clojure, Zig, source-ingestion, and MCP preview coverage documented in
  the [preview capability matrix](../spec/capability_matrix.md).
- A local dogfood proof over a temporary copy of this repository, including a
  Zig member definition and cross-unit call facts.

## Install And Run

This preview is source-built.

From a checkout with Zig 0.16.0, local tree-sitter runtime prerequisites, and
pinned grammar sources:

```sh
./scripts/setup-tree-sitter-grammars.sh
./scripts/check-zig-version.sh
zig build
zig-out/bin/semidx-mcp --version
scripts/semidx-mcp.sh --root /path/to/repository
```

For client setup and tool details, see
[Local MCP preview](../mcp/local_preview.md).

## Source Data Boundary

`semidx-mcp` is a local process. It does not contact a network service while
indexing or answering tool calls.

Source text is off by default. Without `--allow-evidence-text`, MCP results do
not include file contents, function bodies, or snippets. Results do include
source-derived graph values such as paths, entity names, unresolved designators,
ranges, diagnostic messages, resolution metadata, freshness, producers, and ids.

If a hosted MCP client launches the local server, that client may forward
returned graph values to its model or service. semidx cannot see or enforce
what the client does after receiving results.

`--allow-evidence-text` additionally returns each claim's recorded evidence text,
bounded to 400 bytes per claim. Current producers record names or designators as
evidence text, not source bodies.

## Known Limits

- No stable semantic contract or schema set is published.
- No persistence, file watching, daemon lifecycle, HTTP transport, package
  distribution, binary artifacts, resources, prompts, subscriptions, embeddings,
  or ranking.
- Zig cross-unit calls are recorded only through local relative `@import`
  aliases. Calls through receivers and values (`self.x()`), package imports
  (`std.…`), nested namespaces (`alias.Container.foo()`), and container members
  stay unresolved
  ([follow-up 006](../followups/006_zig_cross_unit_and_member_calls.md)).
- A Zig unit importing a relative file that does not exist yet is not
  reanalyzed when the file appears; refresh after editing the importer. Its
  calls through that alias stay unresolved meanwhile, never false facts.
- `semidx_context` responses are bounded per list, not per response, and can be
  large for call-heavy Zig functions (Plan 007 input).
- Clojure symbols stay unresolved wherever a local binding cannot be ruled out
  ([follow-up 008](../followups/008_clojure_lexical_scope_coverage.md)).
- Java same-package type resolution treats the indexed root as one classpath
  ([follow-up 003](../followups/003_java_classpath_boundaries.md)).
- The grammar setup script still fetches unused grammar sources that are not
  needed by this preview path ([follow-up 007](../followups/007_grammar_setup_fetches_unused_unpinned_sources.md)).

## Release Gate Evidence

The release gate ran from a clean worktree at `6e816fc` on 2026-09-17, with the
commands Plan 005 defined for preview.1.

| Command | Result |
| --- | --- |
| `./scripts/check-zig-version.sh` | Pass; Zig 0.16.0 |
| `zig build test-core -Dgrammars-dir=/nonexistent --summary all` | Pass; 89/89 |
| `zig build test --summary all` | Pass; 203 passed, 1 skipped |
| `zig build test-mcp --summary all` | Pass; 18 passed, 1 skipped |
| `zig fmt --check build.zig src tests` | Pass |
| `zig build run -- src` | Pass; exit 0, full output consumed (2,795 lines, no non-printable bytes) |
| `zig build dogfood --summary all` | Pass; 5/5 |
| `zig-out/bin/semidx-mcp --version` | Pass; stdout `semidx-mcp 0.1.0-preview.2`, stderr empty |
| `./scripts/check-agent-attribution.sh --all` | Pass |
| `./scripts/check-memory-freshness.sh` | Pass |
| `git diff --check` | Pass |

The skipped tests are the gated dogfood tests in non-dogfood lanes. The
dogfood lane runs them explicitly. The Plan 006 dogfood observations (member
definition, 23 `writeString` call facts, response sizes) are recorded in the
[Plan 006 progress log](../reports/006_zig_dogfood_semantic_coverage_progress.md#stage-5-dogfood-and-documentation).

## Tagging

`v0.1.0-preview.2` was tagged on 2026-09-17 as an annotated tag at `8818bb6`,
the commit that records this gate evidence (source and build identical to
`6e816fc`, where the gate ran), and pushed to `origin` the same day. No GitHub
release was created.
